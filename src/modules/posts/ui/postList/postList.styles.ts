import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		padding: 16,
		gap: 12,
	},
	emptyContainer: {
		padding: 20,
		alignItems: "center",
	},
	emptyText: {
		fontSize: 16,
	},
});